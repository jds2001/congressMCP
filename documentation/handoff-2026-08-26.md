# Handoff — implementation work, issued 2026-08-26

*(Spec session → implementation session. This is the single consolidated list; the binding contracts live in the files cited. Per `AGENTS.md`: you may read anything, write anywhere except `documentation/`; unit tests for everything; granular per-defect commits with the trailer; do not run the e2e harness.)*

Three packages, in priority order. Package 1 ships alone and first.

## Package 1 — `search_bills` docstring: field enumeration (top priority — misleads a model today)

**Contract:** `govinfo-search-spec.md` **Addendum 4 item 0**, plus the Addendum 3 post-merge measurement note directly above it. **Branch:** new, off master.

- [ ] Probe each not-yet-measured §2a BILLS field with a known-positive query (one call each; `congress:`/`billtype:`/`docnumber:`/`title:`/`shorttitle:` are already measured). Archive under `runs/govinfo-search/`, key-scrubbed as always.
- [ ] Preregistered extra probe: an unrecognized field name — expected silent empty, not an error. Record either way.
- [ ] Docstring enumerates every field that passed its probe, with the **measured** value syntax; a failing field is omitted and reported back, never documented on faith. Add the unrecognized-field line only if the probe confirmed silent-empty.
- [ ] Extend the description-pin test to the field list.
- [ ] Acceptance: `git grep` finds each documented field in `congress_api/features/bills_tool.py`; probe artifacts exist per field.

## Package 2 — F35 + F36 (branch `fix/f36-amends-extraction`, already created)

**Contracts:** work order at the end of `fulltext/14-defect-priority.md`; F35 = `fulltext/05-parsing-addressing.md` F35 blockquote; F36 = `fulltext/06-segments-amendatory.md` **Amendment A8**. One rendering-version bump, one corpus rebuild, both defects together.

- [ ] **Mechanism check before any new pattern:** the P.L.-left-of-USC differential points at the span-claiming step (hypothesis at the F36 entry). Diagnose first — the fix may be a claiming-order correction, not new regex.
- [ ] F36 per A8: parenthetical subject citations under the strict verb hug; `usc_note` as a third `kind` (printed form verbatim, `note` / `note prec.`); both kinds emit when both present; `division N of P.L.` extracts the P.L.; `Stat.` rule unchanged; en-dash and hyphen accepted; order independence.
- [ ] Regression fixtures: HR 1362 §2 (must not regress), HR 7672 §3, HR 4631 §2 (must extract) — enroll if not in the corpus.
- [ ] F35 per §5: structural units below section level render their enum verbatim before the header; rendering-version bump (the §10 tripwire firing is the guards working).
- [ ] One corpus rebuild, then acceptance — all set-based:
  - [ ] `tests/corpus/f36_scan.py` re-run on the **archived input** (`runs/f36/2026-08-22T173903Z/input`, the `--extra` invocation): strict no-entry (830 instances / 689 units) and partial (70) sets drain to zero or a hand-classified residual — **before/after sets in the artifacts, not counts**.
  - [ ] Planted negatives hold: `119hr10115ih` S:9/S:11/S:12 emit nothing; not-hugged (2,330) and provenance (200) stay non-emitting; A6-relaxed (79 units) untouched and out of scope.
  - [ ] Fresh n=20 hand-coded precision sample of newly emitted entries, seed recorded, labeled coder-with-history; every sub-shape from `precision-hand-coding.md` covered or shown absent.
  - [ ] Coherence invariant (`amends != [] ⟹ is_amendatory`) re-run corpus-wide, zero violations.
  - [ ] `119hr10115ih` §12 renders `(e)`/`(f)`/`(g)` and the §12(g)→12(e) cross-reference is checkable from the response alone.
- [ ] Post-fix: **measure** the residual class (amendatory units with an explicit unresolved cite and empty `amends`) and report the number. Do **not** implement `extraction_status` — the spec decides on the measurement.

## Package 3 — Addendum 4 items 1–3 (after packages 1–2, or interleaved at the maintainer's direction)

**Contract:** `govinfo-search-spec.md` Addendum 4. New feature branch off master.

- [ ] **Q11 snippets** (item 1): cache-only opportunistic + `snippet_fetch` (cap 5); per-term localization; the structural tri-state is the make-or-break (empty string unrepresentable); every snippet carries `section_id` and `match_contexts` with quoted-governs in the description. Acceptance A10 is maintainer-run.
- [ ] **Q12 diagnostics** (item 2 — confirm with the maintainer before starting; scheduling is their call): fires only on ≥2 text terms + upstream total 0; `diagnostics` absent unless fired; probe errors are `count: null` + `probe_failed`, never `0`. Acceptance A11 is maintainer-run.
- [ ] **A7 timing gate** (item 3): `timing` emitted only when `CONGRESSMCP_VERBOSE` is set, per fulltext §4 Amendment A7; diagnostic-on-failure blocks stay always-on; README line.

**Reporting:** per package — commits, test counts, and the observable artifacts named above (probe runs, scan set-diffs, fixture ids). Reports are claims; the spec session adjudicates against the artifacts, so name the paths.
