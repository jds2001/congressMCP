# `search_bills` on GovInfo `/search` — spec

**Status: COMPLETE 2026-08-25 and MERGED upstream as #73 (`38c2930` on master, observed from git history) — §7 + Addenda 1–3 discharged (A1–A9 ALL PASS live; F38/F27/F39 closed; D17/D18 closed at the register); §8 carries the consumer-critique triage; Addendum 4 (docstring fields / Q11 snippets / Q12 term-ladder diagnostics / A7 timing gate) **COMPLETE 2026-08-27 — all items adjudicated on live acceptance (A1–A11 ALL PASS, `runs/govinfo-search/2026-08-27T041618Z/`); pending only the merge of `integrate/f36-docstring-addendum4` + the spec branch tip.** Branch `feature/govinfo-search`. **Branch note 2026-08-25: commit SHAs cited in this file are development-history SHAs** — the branch was squashed to one commit for upstream submission (the PR-2 pattern); the 30-commit history is preserved at **`archive/govinfo-search-dev-2026-08-25` on `jds2001/congressMCP`** (the development fork), and SHAs resolve there, not from the upstream squash. **Second archive, 2026-08-25: `archive/govinfo-search-dev-2026-08-25b`** holds the post-rebase history — the consumer-critique triage rounds, the Addendum 3 bounce and its round-2 fix — squashed again for the reopened PR; post-rebase SHAs resolve there. Defects this work closes: **D17 and D18** (`tool-defect-register.md` — read their joint entry first; the differential probe table there is the diagnostic record). The conventions in `fulltext/00-INDEX.md` ("Conventions — these bind") apply here unchanged, including one-line-per-paragraph formatting and measurement-over-assertion — **vendor documentation is a claim with better provenance than an assumption, not a measurement; anything marked "per docs" below still gets a confirming probe before acceptance runs.**

## §1 Mandate — settled 2026-08-24, maintainer

- Replace `search_bills`' recency-window filter with **GovInfo `/search` full-text search restricted to the BILLS collection** as the primary path. #66's honest-window behavior (250-row page, window named in titles, honest miss message) only made the window honest; it is still blind to old bills and name-noise.
- **#66's honest-window behavior is retained as the fallback** when GovInfo is down or unreachable.
- **Reuse the existing keyed GovInfo client** — `X-Api-Key` header + backoff (never key-in-query; `fulltext/09-safety.md` §11 hygiene inherited).
- **Compose with the PR-2 cache**: a search hit can warm straight into `search_bill_text`. (Presumption, pending Q8: the hit *carries the identifiers* a follow-up call needs; no speculative prefetch.)
- Prior art pointer from the maintainer's fix-direction ruling (2026-08-20, D17/D18 entry): `search_bill_text`'s `query_diagnostics` discipline — a zero must be readable — applies at corpus level.

## §2 Constraints carried in from the record — binding

- **The D17/D18 masking constraint.** Satisfied by construction here (the corpus search replaces the window and the matcher in one move), but binding if scope ever shrinks: **no matcher-only intermediate ships.**
- **Matching semantics go in the tool description** — the rule measured into `fulltext/07-search.md`: descriptions shape input; D17's root harm was matching semantics the caller was never told.
- **Three zeros, never one string.** "No match in the corpus," "no match in the fallback window," and "the search errored" are three different responses. A `/search` failure must surface as the §9 envelope or the labeled fallback — never as an empty result set. This is the scan-that-errors rule bound onto the tool itself.
- **§9 error envelope, server-wide** (#68): new code paths emit `error.code`/`message`/`detail`/`remediation`; F22 URL-stripping applies to any GovInfo URL placed in `detail`; keyless is `api_key_missing`-shaped per the F31 contract, never `govinfo_key_rejected`.
- **#65 coherence:** `results_count` equals the length of the typed list it counts, in every response shape including the fallback.
- **Quota:** GovInfo's 36,000/hr bucket is **shared with bill-text content fetches** (M3-confirmed: one request per search call, one counter across `/search` and `/packages`). Deployment reality recorded 2026-08-24 (maintainer): **single-user stdio is the designed mode — the license prohibits SaaS hosting** — so per-call cost is negligible; what survives of this constraint is only that bulk search loops must not starve retrieval.
- **Trace coverage** *(added 2026-08-24, maintainer — omitted from the original issuance)*: the trace mechanism (`CONGRESSMCP_TRACE_DIR`, shipped in PR 1) binds this tool. A traced run must show the assembled upstream query, the upstream outcome, the canary firing and its branch, and any fallback demotion with its trigger class — so §17-style adjudication can attribute behavior instead of inferring it. The key never reaches a trace record (F15/§11: scrub headers; the query string carries no secret by construction).
- **No new dependencies.**

## §2a Upstream interface — per published documentation, 2026-08-24 (probe confirmation owed)

Sources, maintainer-supplied 2026-08-24: `govinfo.gov/features/search-service-overview` (service), `govinfo.gov/help/search-operators` (query language), `govinfo.gov/help/bills` (BILLS fields). The service is labeled **public preview** (launched 2023-08-15) — a stability caveat worth one line in the completion report when this ships.

- **Endpoint:** POST `https://api.govinfo.gov/search`. Requires an API key (`DEMO_KEY` exists for low-volume probing).
- **Request:** `query` (string, the full query language below), `pageSize` (max 1000), `offsetMark` (**cursor**: `*` on the first call, then the value the previous response returned), `sorts` (array of `{field, sortOrder}`; fields `score`/`publishdate`/`title`/`lastModified`; "sorting by score ascending is not supported"), `resultLevel` (`"package"` or `"default"` — default is a mix of granules and packages), `historical` (bool, default false, "include non-current documents").
- **Response:** `count` (total matches) plus records carrying `title`, `packageId`, `granuleId`, `lastModified`, `governmentAuthor`, `dateIssued`, `collectionCode`, `resultLink`, `dateIngested`, and a `download` link object.
- **Query language:** multiple words are an **implied AND** (documented verbatim: "spaces between words are treated as an implied 'and'"); `"quoted phrases"` match adjacent-and-in-order; `AND`/`OR`/`NOT`/`-` booleans; `?`/`*` wildcards; `adj`, `before/#`, `near/#` proximity; `field:value` fielded terms (no spaces around the colon); `field:range(a,b)` ranges. Quotes inside the JSON `query` are backslash-escaped.
- **BILLS fields:** `collection:bills`, `congress:`, `billtype:` (`hr s hjres sjres hconres sconres hres sres`), `docnumber:`, `billversion:`, `title:`, `shorttitle:`, `chamber:`, `member:`, `memberparty:`, `memberstate:`, `committee:`, `actiondate:`, `publishdate:`, `isprivate:`, `isappropriation:`, `uscodecitation:`, `statutecitation:`, `plawcitation:`. Package ids follow `BILLS-{congress}{billtype}{docnumber}{billversion}`.
- **Noted, out of scope here:** `uscodecitation:`/`plawcitation:`/`statutecitation:` are an upstream affordance adjacent to the `amends` work (F36's citation classes, queryable corpus-wide). Recorded so it is not rediscovered; not part of this fix.

**What the documentation changes:** implied-AND semantics are the *opposite* of D17's OR-substring pathology, so the upstream default is already the correct matcher; the design work is mostly honesty plumbing and pagination. The load-bearing unknowns left for probes: tokenization of punctuated terms (`St.` in A1's query), what `historical` means for bill *versions* (it may do Q3's dedup upstream), granule-vs-package behavior for BILLS under each `resultLevel`, and cursor stability.

## §2b Measured behavior — probe run 2026-08-24 (`runs/govinfo-search/2026-08-24T234232Z/`)

29 keyed calls against the live endpoint by this session (maintainer-supplied key; probe script archived in the run dir; the key appears in no artifact — grep-verified before commit). Every §3 prediction was exercised; three documented-or-assumed behaviors were falsified, and per the preregistration those are findings recorded here.

**Confirmed:**

- **Implied AND, exactly as documented.** The full unquoted A1 title → `count: 1`, exactly `BILLS-119hr4631ih`. The dot-stripped `St Louis` variant is identical — unquoted tokenization forgives punctuation.
- **A2/A3/A4 as predicted:** 31 / 31 / 0. The D17 differential is structurally dead — adding or dropping `Act` changes nothing, and the 31 are RECA bills, not a newest-bills page.
- **`RECA` keyword → 6 hits including both `119hr4631ih` and `119hr10115ih`** — A8's real-use case is reachable upstream.
- **Fielded `congress:`/`billtype:`/`docnumber:` returns exactly the bill's version set** — S. 1071 → all four versions (`is`/`es`/`enr`/`eah`). The version-discovery affordance is live, for free.
- **Cursor pagination is sound:** pages disjoint, and reusing a 90-second-old `offsetMark` reproduces page 2 identically — cursors are stable and replayable, which is what Q4's page-token option needs. **An exhausted page still returns a non-null `offsetMark`** (the `count: 1` probe carried one; only the zero-hit query returned null — adjudicated from the same run's artifacts, 2026-08-24), so end-of-results must be **computed** from records-consumed vs `count`, never inferred from cursor nullness.
- **Shared quota bucket confirmed:** a `/packages` summary call decremented the same `X-RateLimit-Remaining` counter as `/search` (limit 36,000); one request per search call.
- BILLS records are package-level regardless of `resultLevel` (`granuleId: null`), and `historical` true/false are identical on S. 1071 — both knobs are no-ops for this collection.

**Falsified / surprises — all load-bearing:**

1. **Quoted phrases miss bills whose *titles* carry the phrase.** `"St. Louis RECA Readjustment Act"` → 0 while the same words unquoted → 1; `"RECA Readjustment Act"` → 0; `"Readjustment Act"` → 2 hits *not* including hr4631. Yet `"Radiation Exposure Compensation Act"` → 4 (body-text occurrences), and fielded `shorttitle:"St. Louis RECA Readjustment Act"` → 1 exact. The mechanism is uncharacterized (title text under phrase-adjacency indexing); the operational rule does not depend on it: **phrase queries are unreliable against title text — plain AND terms, or `title:`/`shorttitle:` fields, are the reliable forms.**
2. **The default sort is not relevance.** No-`sorts` ordering differs from explicit `score DESC` and looks package-id-shaped; the explicit ordering is visibly relevance-like (the two RECA bills lead). The docs never actually stated a default — the assumption was ours.
3. **Malformed requests wear the outage shape.** Bad `offsetMark`, bad `sorts`, non-numeric `congress:`, and a ~900-term query all return the same generic 500 (`"Oops, Something went wrong…"`) — indistinguishable from GovInfo-down at the HTTP level.
4. **An empty query matches the entire corpus** — `count: 8,759,900`, cross-collection, granule-level records, HTTP 200.

## §3 Preregistered acceptance floor — from the D17/D18 record

Each probe archives artifacts (`runs/` convention) and asserts a non-zero denominator. A1–A4 are the register's regression probes; A5–A8 kill D18's structural consequences and the real-use failure.

- **A1:** `search_bills(congress=119, keywords="St. Louis RECA Readjustment Act")` returns HR 4631 — the bill unreachable by its own exact title today.
- **A2:** `Radiation Exposure Compensation` (no "Act") returns relevant bills, non-empty — the query that returns 0 today.
- **A3:** the D17 differential is dead: the two RECA queries must not return byte-identical newest-bill lists, and dropping `Act` must not zero a query whose other terms match.
- **A4:** `zzzqqx` → honest zero with corpus-level diagnostics, not an error and not noise.
- **A5:** monotonicity — the result set at `limit=10` is a prefix of the ranking at `limit=50` for the same query (D18's non-monotonicity dead).
- **A6:** pagination pages the *result set*: matches enumerable across pages without duplication or loss (within upstream index stability across the run). *Restated from "offset pages the result set" — the upstream is cursor-based (§2a), and the surface question is Q4.* *Amended with §6.3: the one tolerated duplication class is a bill whose version records straddle an upstream page boundary — disclosed, same bill identity both times.*
- **A7:** fallback cell — GovInfo unreachable (poisoned proxy, per the V11/step-5 technique) → labeled honest-window response, distinguishable from a corpus zero by structured metadata, not prose.
- **A8:** the 2026-08-22 real-use failure: `119hr10115ih` reachable by title keywords — the case where a live research session had to route around `search_bills` entirely.
- **A9** *(added 2026-08-25 with Q10; wording corrected same day — see below)*: time-bounding through the tool — the RECA query bounded to 2025 returns **the exact bill set {hr1, hr1362, hr4631, s243} — four bills, five version packages** (`results_count: 4`, `total_version_matches: 5`; HR 1 matched in both `enr` and `eas`, deduped, `enr` fronted) — without `hr10115ih`; bounded to 2026 returns exactly `hr10115ih`; a single-day bound on 2025-07-23 returns exactly `hr4631ih` (inclusivity); one-sided bounds behave; a fallback response under bounds names the `updateDate` semantics with structured `date_bounds`. **Correction record:** this line originally said "the five 2025-dated bills" — M5's raw *record* count carried onto the word "bills," the exact granularity confusion `total_version_matches` was named to prevent, committed by this session in its own acceptance text (the F33 acceptance-sentence family, second instance). The first live run failed against the wrong wording and the artifact decided (`runs/govinfo-search/2026-08-25T032638Z/` preserved failing, `…032736Z/A9_2025.json` the correct set) — **the tool was right, the sentence was wrong; the harness's granularity pin (bill set of four AND packages == 5) is RATIFIED.** **A9 PASS 2026-08-25, with A1–A8 re-confirmed in the same run.**

Under §2a's documented semantics, A1–A4 all have *predicted* outcomes (implied AND over title words → HR 4631 reachable; AND over three real terms → non-empty; the differential structurally impossible; `zzzqqx` → `count: 0`). The predictions are preregistered by this paragraph: if a probe falsifies one, that is a finding about the documentation, and it gets recorded here either way. **Outcomes 2026-08-24 (§2b): A1 unquoted CONFIRMED at `count: 1` exact; A2 CONFIRMED (31, relevant); A3 CONFIRMED dead; A4 CONFIRMED clean zero; A8's premise CONFIRMED upstream. The quoted-phrase A1 variant fired the falsifier clause — §2b item 1.** A5–A7 are tool-level and remain for acceptance. **Acceptance run 2026-08-25 (`runs/govinfo-search/2026-08-25T015806Z/`, harness run by the maintainer, artifacts read by this session): A1–A6 and A8 PASS** — A1 `count: 1` exact; A5 prefix holds (n=10 within n=26); A6 four-page walk, 27 bills, zero duplicate ids. **A7 VOID, not failed:** the proxy poison caught both legs — the artifact's nested `fallback_error` shows the congress.gov fetch dying after 4 attempts — so the cell's premise (GovInfo down, congress.gov alive) was never established; F23's rule on the environment axis. The response the tool gave under genuine both-legs-down is correct and is ratified as a §6.4 row. **Owed: a host-selective poison** — a real proxy refusing `api.govinfo.gov` and tunneling congress.gov through (maintainer-concurred 2026-08-25; stdlib-implementable, no-new-deps holds) — **plus cell premise assertions** (under poison, one congress.gov canary must succeed *and* one GovInfo call must fail, else the cell halts VOID rather than scoring); then the A7 re-run. **A7 re-run 2026-08-25: PASS — A1–A8 now ALL PASS** (`runs/govinfo-search/2026-08-25T022839Z/`, key grep-verified absent by this session). The fixed instrument is the ruled shape: host-selective stdlib CONNECT proxy (refuses `api.govinfo.gov` at CONNECT before any egress, tunnels everything else), premise assertions gating the probe, and the artifact carries the full premise record (`govinfo_dead: true` via ProxyError, `congress_alive: true` through the tunnel, per-host deny/tunnel logs). The response carries every §6.4 obligation: `search_source: recency_window_fallback`, `fallback_trigger: govinfo_unreachable`, window metadata (250 scanned, oldest 2025-02-07, truncated), coherent `results_count`, and an honest message naming what the window is not. **Instrument vindication worth recording:** the first fixed-instrument run failed A7 for a *new* reason — the harness's own bare client lacked the lifespan client's `base_url`, so the fallback leg died harness-side — and the premise evidence localized it in one artifact read; under the old blanket poison that failure was unattributable by construction.

## §4 Open design questions

Routing per `documentation/CLAUDE.md`: **RULE HERE** = IR/technical judgment, decided in this file with rationale; **MAINTAINER** = requirements/product. Gating measurements marked.

- **Q1 — surface.** Same `search_bills` operation, signature-preserved, is the presumption (the mandate names the tool). MAINTAINER only if a parameter must break — **Q4 now invokes exactly this carve-out.**
- **Q2 — query semantics. RULED 2026-08-24 on §2b.** **Pass the caller's `keywords` through to `query` unmodified**, server appending the scoping terms (`collection:bills`, `congress:{n}` when set) and always appending explicit `sorts` (Q9). **Blank or whitespace-only `keywords` are rejected client-side with an `invalid_parameters` envelope, never sent** — an empty upstream query matches 8.76 M documents (§2b item 4). No silent rewriting; the JSON transport escape is the only server-side transformation. The tool description states the measured semantics: words are ANDed; **do not quote bill names — quoted phrases are measured to miss title text** (§2b item 1); `title:"…"`/`shorttitle:"…"` are the exact-title forms and pass through; `OR`/`NOT` available. *Deferred option, recorded not built:* disclosed server-side augmentation `(<kw>) OR shorttitle:"<kw>"` could rescue phrase-shaped intent, but unquoted AND already resolved every measured case at `count: 1` — simplicity holds until a real miss is observed.
- **Q3 — version multiplicity. RULED 2026-08-24 on §2b: dedup is client-side** — `historical` is measured a no-op for BILLS and the multiplicity is real (S. 1071 → four records). Group result records by `(congress, billtype, docnumber)` parsed from `packageId` (`fulltext/00-INDEX.md`: carry the authoritative value — the id is in hand, do not re-derive from other fields); front the most authoritative version per `fulltext/03-data-sources.md` §3 precedence; **carry the full per-bill version list in the hit** — the records are already in hand, the affordance is free. **Version discovery: RULED partial discharge (maintainer, 2026-08-24)** — search hits become the affirmative version-discovery surface, `version_not_available` keeps covering the pinned-version error case, and the tool description must teach the affirmative path explicitly. **One precision correction folded in by this session, same day: a keyword hit carries only the versions whose *text matched* the query — the field is named `matched_versions` so it cannot be misread as the complete set.** The complete set is available through the same tool by disclosed usage: a fielded `congress:{n} billtype:{t} docnumber:{m}` query with no text terms returns exactly the bill's versions (measured, S. 1071 → 4/4, §2b) — so the maintainer's floated versions-only query is already reachable via pass-through, the description teaches the form (§6.5), and a dedicated mode is **deferred, gated on an observed consumer stumble** over the extraneous-results shape.

- **Q4 — pagination surface. RESHAPED by §2a: upstream is a cursor (`offsetMark`), not an offset — random access does not exist.** D18 already established the shipped `offset` is incoherent (it pages the candidate window), so no consumer can be depending on correct offset behavior. Options: (a) replace `offset` with an opaque page-token parameter mirroring the cursor — honest, cheap, **breaking**; (b) keep `offset` and emulate by walking cursors server-side — non-breaking, cost linear in offset, and it re-manufactures random-access semantics upstream doesn't offer; (c) drop pagination and cap results. **Recommendation: (a)** — the parameter is broken today, so the break is nominal and the honesty is real. §2b adds the supporting evidence: cursors are stable and replayable at ≥90 s with disjoint pages, so an opaque token is a faithful wrapper, not a leaky one. **SETTLED (maintainer, 2026-08-24): option (a).** The signature change is approved as the honest path given that random access does not exist upstream; emulating it was rejected as expensive and purposeless. Contract in §6.3.
- **Q5 — filter composition** *(gated M1)*. `congress` and bill type map to fielded terms (`congress:`, `billtype:`) — upstream, settled. **Policy area has no GovInfo field** (§2a's list is exhaustive per the docs): options are dropping the filter for the corpus path, post-filtering the corpus result set (unlike D18's shape this filters *matches*, not a recency page — but it breaks `count` honesty and pagination), or keeping it fallback-only. **SETTLED (maintainer, 2026-08-24): dropped from the corpus path — and since a filter that binds only in fallback mode is a trap, the parameter leaves `search_bills` entirely (§6.1).** *Deferred idea, recorded so it is not rediscovered:* a congress.gov-backed policy-area search as a separate tool, limitations disclosed — out of scope for this fix.

- **Q6 — response shape.** Keep the existing `search_bills` response model; add source disclosure — a machine-readable marker distinguishing corpus results from fallback-window results, plus the D17/D18 honesty metadata (`bills_scanned`, oldest `updateDate`, `window_truncated`) whenever the fallback fires. Corpus responses carry upstream `count` (total matches) beside the returned page so a truncated list is legible. #65 coherence binds: `results_count` counts the returned list, not upstream `count`. **RULED, maintainer concurring 2026-08-24; the response metadata is a mandatory tool-description item (§6.5) so the consumer knows what it is reading.** Contract in §6.2.

- **Q7 — fallback trigger. RULED 2026-08-24 on §2b item 3, which reshaped it: a 500 cannot be read as "GovInfo down" — it is also what a malformed request returns, with an identical body.** (i) **Strict construction:** the only unvalidated content in `query` is the caller's keywords; every server-built term (`sorts`, `pageSize`, `offsetMark`, scoping) is validated before send, so a 500 implicates the endpoint or the caller's own syntax, never our plumbing. (ii) **Trigger classes, all labeled in the fallback metadata:** HTTP 500 → fallback fires as `govinfo_search_error`, remediation naming the ambiguity ("the query may use unsupported syntax, or the search service is failing — simplify the query to plain words to distinguish"); network unreachable/timeout → fallback as `govinfo_unreachable`; 429 → fallback as `govinfo_rate_limited`; keyless → **no fallback**, `api_key_missing` envelope per F31 (the operator must act). **Amended 2026-08-24 (maintainer): the canary is ruled IN, reversing the deferral above.** Rationale accepted and recorded: the server's designed deployment is **single-user stdio — the license prohibits SaaS hosting** — and a search costs one call against a 36,000/hr bucket, so a canary is near-free and buys real diagnosability. Corroborating instance, independently observed by the maintainer: a nonexistent operator typed into GovInfo's own swagger UI produced the same unhelpful generic 500 (§2b item 3's shape, from a third instrument). The canary changes the 500 branch: canary succeeds → the caller's input is implicated → **no fallback**, a `govinfo_query_error` envelope (remediation: plain words, no operators; when a `page_token` was supplied, it is named as the other suspect); canary fails → outage confirmed → fallback as `govinfo_search_error`. Normative flow table in §6.4.

- **Q8 — cache composition.** Presumption recorded in §1: the hit carries package/version identifiers (upstream `packageId` decomposes per §2a's id structure); no prefetch (quota). **SETTLED (maintainer, 2026-08-24): no speculative prefetch.** The confirming rationale is recorded: which hits would even warrant it — the top hit? all of them? — a page of never-consulted bills would evict genuinely warm entries from the cache's 500 MB LRU. The hit carries the identifiers; warming happens when the consumer actually calls `search_bill_text`.

- **Q9 — ranking. RULED 2026-08-24, and §2b item 2 upgraded it from preference to necessity: the upstream default is NOT relevance** (no-`sorts` ordering is package-id-shaped). **Every request sends explicit `sorts: [{score, DESC}]`.** No caller-facing sort parameter (the fallback window is already recency-shaped); revisit only on a real request.
- **Q10 — time-bounding. RULED 2026-08-25 (maintainer directive reversing part of the §6.1 removals, + M5 measurements).** The maintainer's call: time-bounding is supported upstream and was advertised surface — support it. **`fromDateTime`/`toDateTime` are restored, mapped to `publishdate:range(from,to)` on the corpus path.** M5 measured every mapping property against known-date bills (run `runs/govinfo-search/*-m5/`, 7 probes, all preregistered predictions held): range **inclusive both ends** (single-day range returns the exact-date bill), **both one-sided forms native** (`range(from,)` and `range(,to)` both correct), year form `publishdate:2026` works, and a datetime string inside a range is tolerated upstream. Ruling details: inputs validated client-side as ISO date or datetime, **datetime truncated to date** before assembly (tolerated upstream, but the canonical query keeps the description honest), `from ≤ to` enforced, one-sided passes through natively; **semantic shift disclosed** — the corpus path bounds the *version document's publication date* (`dateIssued`), not congress.gov's `updateDate`, and a bill matches when any text-matching version falls in range; **the fallback applies the same bounds client-side over the window rows' `updateDate`** (the nearest available field), with the difference named in the fallback message. `format` stays removed (ratified — an output-shape artifact with no corpus meaning); `sort` stays removed per Q9.
- **Q11 — local snippets (RULED 2026-08-25; maintainer-approved direction, consumer-proposed; NOT in the reopened PR — next batch).** Upstream has no snippets (§8 measured), but this server owns the *other* index: for a hit whose bill is retrievable locally, run the caller's terms against the FTS5 segments and attach tool-generated match contexts as a triage snippet. Two composable modes, both maintainer-named: **(a) opportunistic, always on, cache-only** — a hit whose package is already in the persistent cache gets a snippet for free, no network; **(b) opt-in bounded fetch** — an explicit request parameter (`snippet_fetch: N`, hard cap small, e.g. ≤ 5) additionally downloads-and-parses the top-N uncached hits, spending real cost only on caller demand (consistent with Q8: this is requested warming, not speculation — and it leaves those bills warm for the follow-up reads a research session makes anyway). **Binding disambiguation contract (the make-or-break, and the three-zeros rule again):** the snippet field is tri-state and the states must be structurally distinct — a snippet; `snippet: null` + a marker meaning *upstream match not localized* (the semantics mismatch is real: GovInfo ANDs raw words, FTS5 matches stemmed phrases, so a genuine upstream hit can fail local localization — localize per-term, not as a phrase, to narrow the gap); and absent-because-not-attempted (uncached, no opt-in). An empty string masquerading as "no match" recreates exactly the silent-zero ambiguity this tool exists to kill; if the tri-state cannot be kept clean, the feature does not ship.
- **Q12 — starved-result diagnostics (deferred, refined 2026-08-25; GO 2026-08-27 with the maintainer's ladder design superseding the per-term shape — Addendum 4 item 2 as amended):** per-term document-frequency probes on low-count results, surfacing as a **structured field** (not prose), firing **only for multi-term queries** (a one-term zero needs no explanation and is where the extra calls would be wasted). Cost: one cheap call per term, near-free under the Q7 deployment reality. Maintainer's call on scheduling; not in the reopened PR.

## §5 Measurements owed before design freeze

**ALL FOUR RAN AND ADJUDICATED 2026-08-24** — this session, maintainer-supplied key, artifacts `runs/govinfo-search/2026-08-24T234232Z/` (29 calls, probe script archived in the run dir, key in no artifact). Results in §2b; rulings applied in §4. Residual, explicitly not owed for design: the *mechanism* of the phrase-vs-title miss (§2b item 1) is uncharacterized — the ruling routes around it rather than depending on it.

- **M1 — query language: DONE.** Predictions confirmed except the two §2b falsifiers (quoted-phrase-vs-title; default sort). Fielded terms, punctuation tolerance, `resultLevel`/`historical` no-ops, and version multiplicity all measured.
- **M2 — pagination: DONE.** Three-page cursor walk on a 3,960-hit query: disjoint pages, 90 s cursor reuse reproduces its page exactly.
- **M3 — quota: DONE.** One request per search call; `/search` and `/packages` share the 36,000/hr bucket (one counter, observed decrementing across both).
- **M4 — failure shapes: DONE.** Four malformed-request classes → one generic 500; empty query → 200 over the whole corpus. Both fed directly into Q2/Q7's rulings.
- **M5 — `publishdate:range` mapping (added and DONE 2026-08-25, for Q10).** Seven probes against known-date bills, every preregistered prediction held: inclusive both ends, both one-sided forms native, year granularity works, datetime input tolerated. Artifacts `runs/govinfo-search/*-m5/`.

## §6 Tool contract — the design (2026-08-24, written with every question settled)

Normative for the implementation. Parameters are named by role — retained parameters keep their shipped names, and this session does not read implementation source, so the mapping onto the existing module layout is the implementer's.

### §6.1 Request

- `keywords` — required. Stripped; blank → `invalid_parameters` envelope, never sent upstream (Q2). Otherwise passed through verbatim into `query`.
- `congress` — optional; validated as a positive integer **here** (a non-numeric value 500s upstream, §2b) → appends `congress:{n}`.
- The bill-type parameter (if the shipped tool carries one) — validated against the eight documented values → appends `billtype:{t}`.
- `limit` — output cap in **bills**; existing clamp discipline applies (out-of-range clamps with the shared validator's advisory wording).
- `page_token` — optional; **replaces `offset`** (Q4). Opaque; contents §6.3. A malformed or expired token surfaces through the 500→canary path (§6.4), whose remediation names the token.
- The policy-area parameter is **removed** (Q5). *Corrected 2026-08-25 at implementation: the ruling was vacuous as written — no policy-area **parameter** existed; it was matcher behavior inside the window filter, retained in the fallback (the check-dead-defensive rule, missed by this session). What actually left the surface: `offset` (Q4), `sort` (covered by Q9's no-sort ruling), and `format`/`fromDateTime`/`toDateTime` — window-tool artifacts removed on implementation initiative.* **Ratification landed 2026-08-25 (Q10): `format` removal ratified; `fromDateTime`/`toDateTime` RESTORED** — validated ISO date/datetime (datetime truncated to date, `from ≤ to`), assembled as `publishdate:range(from,to)` with native one-sided forms; fallback path filters `updateDate` client-side with the semantic difference named. Measured mapping properties at Q10.
- Assembled upstream request: `query = "{keywords} collection:bills[ congress:{n}][ billtype:{t}]"`; `sorts: [{"field": "score", "sortOrder": "DESC"}]` always (Q9); `pageSize` per §6.3; `offsetMark` from the token, else `*`; `resultLevel` and `historical` **omitted** — measured no-ops for BILLS (§2b); nothing is sent that does nothing.

### §6.2 Response

- Hits are **bills**, deduped from upstream version-package records in rank order (first occurrence wins), grouped on `(congress, billtype, docnumber)` **parsed from `packageId`** — the carry-don't-reconstruct rule binds: the id is authoritative, nothing is re-derived from other fields.
- Each hit carries: the bill identity; the **fronted version** — most authoritative per `fulltext/03-data-sources.md` §3 precedence *among this bill's fetched records* — with its `packageId`, `title`, `dateIssued`; and **`matched_versions`** — every version of this bill present in the fetched records, precedence-ordered. Named `matched_versions`, not `versions`: a keyword query returns only versions whose text matched, so the list is match-scoped by construction; the complete set comes from the fielded no-text query (§6.5 item 3).
- `results_count` = length of the returned hit list (#65).
- `total_version_matches` = upstream `count`, named for its real granularity: it counts version packages and can exceed the number of distinct bills; the description says so (Q6).
- `search_source`: `"govinfo_fulltext"` or `"recency_window_fallback"`; fallback responses additionally carry the trigger class (§6.4) and the window honesty metadata (`bills_scanned`, oldest `updateDate` in the window, `window_truncated`).
- `next_page_token`: present while unconsumed upstream records remain; `null` when exhausted. Exhaustion is **computed** — `records_consumed ≥ count` — never inferred from cursor nullness (§2b: an exhausted page still returns a cursor; only a zero-hit query returns null).

### §6.3 Pagination and dedup mechanics

- The token is server-encoded and opaque to the caller: `{offsetMark, records_consumed}`. `records_consumed` accumulates upstream records across the walk and is the exhaustion numerator against `count`. **Amended 2026-08-25 — implementation-found, RATIFIED: the token carries a third component, `skip`.** As originally written, whole-page consumption plus the `limit` cap **loses bills** whenever a page dedups to *more* than `limit` bills (a 3×limit page of single-version bills dedups to 3×limit; A6 forbids exactly this loss). Spec-caused — the F33 family: a ruling correct on the shape it was written against (dedup *shrinkage*) and wrong on the adjacent one (dedup *overshoot*). `skip` resumes intra-page on the measured-replayable cursor; `skip = 0` degenerates to the two-field shape, and the resume boundary stays inside the one tolerated same-identity duplication class. Failing shape pinned in `test_overshoot_page_consumes_only_selected_bills_records`; lossless walk in `test_full_walk_over_one_page_is_lossless_and_disjoint`.
- **One upstream fetch per tool call** — predictable cost, one request. `pageSize = min(3 × limit, 300)`: the overfetch absorbs dedup shrinkage (most bills carry 1–4 versions).
- **Short pages are legal:** when a version-heavy page dedups below `limit`, the call returns what it has with `next_page_token` set. Disclosed in the description (§6.5).
- **The one tolerated duplication class:** a bill whose version records straddle an upstream page boundary reappears on the next page (per-call dedup holds no cross-call state). Overfetch makes it rare; the description discloses it; both appearances carry the same bill identity, so a consumer dedups by id trivially. A6's acceptance treats this class as the only passable duplication.
- A5's monotonicity holds by construction — identical upstream ranking, rank-order dedup, constant overfetch factor — and is still measured at acceptance, not assumed.

### §6.4 Failure flow (Q7 as amended — canary in)

| main-call outcome | action | response |
|---|---|---|
| 200 | — | normal corpus response |
| 500 | **canary:** one server-built constant query, known valid, zero caller input | canary 200 → **no fallback** — `govinfo_query_error` envelope (caller's input implicated; remediation: plain words, no operators; names the `page_token` as the other suspect when one was supplied). canary non-200 → outage confirmed → fallback as `govinfo_search_error` |
| network unreachable / timeout | no canary | fallback as `govinfo_unreachable` |
| 429 | no canary (it would spend quota to confirm quota) | fallback as `govinfo_rate_limited` |
| no key configured | — | `api_key_missing` envelope per F31; **no fallback** |

Every fallback response is the #66 honest window wearing `search_source: "recency_window_fallback"`, its trigger class, and the window metadata — the §2 three-zeros rule discharged by structure, not prose.

**Row added 2026-08-25 — ratified from the A7 artifact (the shape was unspecified; the shipped behavior exceeded the spec and got it right): both legs down.** When the corpus call fails *and* the fallback fetch itself fails, the tool returns a §9 envelope — the primary trigger code at top level (`govinfo_unreachable` in the observed instance), a message naming **both** failures, the fallback's own error nested in `detail`, retry remediation. Never an empty success — the three-zeros rule's fourth shape. *Nit, non-blocking: the observed nested `fallback_error` is a stringified envelope; prefer a structured object when next touched.*

*Added 2026-08-24:* every row of this table is trace-visible under `CONGRESSMCP_TRACE_DIR` (§2 trace constraint) — in particular the canary call and its branch, and the moment of fallback demotion; a trace that shows only the final response cannot adjudicate which row fired.

### §6.5 Tool description — mandatory content

The description is load-bearing (§2). The implementer words it; every item below must be present:

1. What the corpus is: full text of congressional bills, all versions, searched at GovInfo.
2. Matching semantics — **normative text adopted verbatim 2026-08-25** (consumer-drafted, maintainer-approved; **the one item shipping in the reopened PR**). The docstring's matching-semantics bullet becomes:

   > Matching semantics: words are ANDed -- every term must appear in the SAME document, so each added word strictly shrinks the result set and can never grow it. Start with the distinctive minimum: the fewest words that name the thing, and add terms only to cut a set that came back too large. Do NOT add words describing the topic, category, or what the bill does -- that is the natural way to phrase a search and the usual cause of a starved result. Worked example: "Radiation Exposure Compensation Act amendments downwinders" returns 1 bill; dropping the two description words returns 26, including the enacted vehicle. A small count means the terms rarely co-occur, NOT that few such bills exist -- re-query broader (drop terms) before concluding anything. Synonyms do NOT broaden here: unlike search_bill_text, which ORs its queries array and rewards adding alternate phrasings, an added synonym on this path intersects and discards.

   The no-quoted-bill-names warning, `title:`/`shorttitle:` forms, and operator pass-through stay alongside it. Three properties are deliberate and survive any rewording: the **worked example with magnitudes** (the abstract AND statement was already in the docstring, was read, and was ignored — 26→1 is what stops the behavior), the **starvation clause** (the damage is a small count treated as a finding — the `recency_window_fallback` principle on a different failure path), and the **cross-tool boundary line** (`search_bill_text` ORs its queries and rewards synonyms; this tool intersects and discards; the habit carries across two adjacent tools and nothing else marks the boundary — warning placed only on the silent-failure side, deliberately no reciprocal pointer).

   > **Correction, 2026-08-27 (implementation session found it; spec amended to match).** The adopted blockquote said "re-query **narrower**" — backwards: under AND semantics the remedy for a starved result is *dropping* terms, exactly what the worked example demonstrates (1 → 26 by removing words). The docstring is corrected in `3e92bf2` ("re-query broader (drop terms)") and the blockquote above now carries the corrected wording; the pin test pins the corrected phrase. The error was in the consumer-drafted normative text itself and survived adoption, Addendum 3's round-2 acceptance, and this spec session's post-merge phrase map — which scored the phrase 0 hits and attributed it to re-wrap drift, when the true causes were an 80-column wrap splitting the phrase *and* the phrase being wrong. A pinned-verbatim defect propagates verbatim; the fix is recorded here so the spec and the wire cannot diverge again on this sentence.
3. **Version discovery:** hits carry `matched_versions`; for a specific bill's *complete* version set, query `congress:{n} billtype:{t} docnumber:{m}` with no text terms (measured: returns exactly the version set). This is the affirmative discovery path (Q3 ruling); `version_not_available` remains the pinned-version error's disclosure.
4. Count semantics: `total_version_matches` counts version packages; `results_count` counts returned bills.
5. Pagination: pass `next_page_token` back verbatim; pages can run short; a bill can rarely reappear across pages (same id).
6. Fallback: when `search_source` is `"recency_window_fallback"`, results are the most-recently-updated window, not the corpus — read the trigger class.
7. Blank `keywords` are rejected.
8. Time-bounding: `fromDateTime`/`toDateTime` bound the **version's publication date** on the corpus path (inclusive; either side optional) — not congress.gov's update date; in fallback mode they bound `updateDate` over the window instead, and the response says so.

### §6.6 Acceptance and test obligations

- §3's A1–A8 as preregistered (A6 as amended).
- The D17/D18 characterization tests from `tool-defect-register.md` land with the fix, per the register's standing rule.
- Unit obligations, **each defense tested in isolation** (`fulltext/00-INDEX.md`): blank-keyword rejection; query assembly (scoping and sorts always present); the dedup grouping property as a **set comparison, not counts** — the fetched-record set equals the union over hits of `{bill} × matched_versions`; precedence fronting; token round-trip and computed exhaustion, including the non-null-cursor-on-last-page shape; every §6.4 row with the canary branching both ways; fallback labeling carrying trigger class and metadata; #65 coherence on both response shapes.

**Handoff:** the ordered work order is §7.

## §7 Implementation handoff — work order, issued 2026-08-24

For the implementation session. §6 is the contract; this is the order. **Commit per step** (repo convention: clear history over a clean log); unit tests land with the step they cover, each defense tested in isolation. The implementation session does not write into `documentation/`; artifacts go under the repo's `runs/` (gitignored — cite paths in the report). Reports name observable artifacts — failing inputs, before/after sets, wire captures — not summaries.

1. **Client + query assembly (§6.1).** `/search` POST on the existing keyed GovInfo client (`X-Api-Key` header, backoff). Assembly: validated scoping (`congress` positive-int, bill type against the eight documented values), always-explicit `sorts: [{score, DESC}]`, `pageSize = min(3 × limit, 300)`, `resultLevel`/`historical` omitted. Blank/whitespace `keywords` → `invalid_parameters` envelope, never sent. Tests: assembly properties; blank rejection; the non-numeric-`congress` guard.
2. **Response mapping (§6.2).** Bill-level dedup in rank order grouped on `packageId` parse (carry, don't reconstruct); precedence fronting per the shipped `fulltext` §3 53-code table; `matched_versions` precedence-ordered; `results_count` / `total_version_matches`; `search_source` marker. Tests: the grouping property as a **set comparison** (fetched records == union over hits of `{bill} × matched_versions`); precedence fronting including a dateless-version shape.
3. **Pagination (§6.3).** Opaque token `{offsetMark, records_consumed}`; `next_page_token` null **only** when `records_consumed ≥ count`; `offset` and the policy-area parameter **removed** (schemas, router, exports, audit allowlist — the #66 removal pattern). Tests: token round-trip; computed exhaustion including the non-null-cursor-on-last-page shape (§2b); short pages.
4. **Failure flow (§6.4).** All five rows; the canary (server-built constant query, zero caller input); fallback = the #66 honest window wearing `search_source: "recency_window_fallback"` + trigger class + window metadata; keyless → `api_key_missing` per F31, no fallback. Tests: every row; canary branching both ways; fallback labeling; a scrubbed-env keyless call (the F31 instrument).
5. **Tool description (§6.5).** All seven mandatory items; check the shipped text against the §6.5 list item-by-item — an enumeration whose members are not individually pinned is the assumption it exists to reject.
6. **Acceptance (§6.6).** Live A1–A8 run with artifacts (A7 via poisoned proxy, the V11/step-5 technique); the D17/D18 register regression probes land as characterization tests (the two asserts in their entry). Suite green; report per-probe artifacts.

**Ride-alongs — cheap because the server is live under your hands anyway; wire captures, not summaries:**

- **F38 check:** malformed `version` (e.g. `"e nr!"`) against `get_bill_section` and `get_bill_toc`; capture both envelopes and report **which code string ships** — §9 says `version_not_available`, the #67 commit title says "version_not_found". Do not rename anything; report, the spec session reconciles.
- **F27 check (#68 adjudication evidence):** capture one str-tool error envelope and one structured-tool error envelope on the wire; report whether the two legacy-shape-pinning static tests were retired in #68; one captured `detail` carrying a URL, showing F22 query-stripping.

**Explicitly out — do not touch:**

- F35 + F36 (parser-side pair; separate batch with its own rendering-version bump).
- The deferred options recorded above: the versions-only query mode (Q3), `shorttitle:` augmentation (Q2), the congress.gov policy-area tool (Q5), speculative prefetch (Q8), the on-500 fallback canary variants beyond §6.4.
- Any change to the bill-text tools or the cache beyond reusing the client (except the two ride-along *captures*, which change nothing).
- The #17 requirements call; anything in `fulltext/` — that surface is closed for this work order.
- No new dependencies (standing).

**Addendum, 2026-08-24 (maintainer, after issuance — work already in flight, so this is an addendum rather than a renumbering):** the trace mechanism (`CONGRESSMCP_TRACE_DIR`) must cover this tool — assembled query, upstream outcome, canary firing + branch, fallback demotion + trigger class, key scrubbed from all records (F15/§11). Lands naturally with step 4; test obligation: one traced call per §6.4 row shows the row that fired. §2's trace-coverage constraint and §6.4's trace note are the contract.

**Adjudication, 2026-08-25 (this session).** Steps 1–5 + trace addendum: report received (10 commits `63b96e7..f1eee63`, 195 new tests) and the suite claim **reproduced black-box** — the known-failures clamp run by this session prints the identical baseline (2 known failures, 4 known collection errors, nothing new; exit 0). Step 6: the harness shipped key-gated per the standing convention; the maintainer ran it — outcomes recorded in §3. Both ride-alongs **CLOSED** at their entries (F38, F27 — `fulltext/14-defect-priority.md`; §9 amended with the `version_not_found`/`version_not_available` split). The token deviation is **RATIFIED** as §6.3's `skip` amendment; the Q5 vacuity is corrected in §6.1. The 3.10-floor work and the two en-route fixes (the `AsyncMock` signature crash `c06b2f9`, the import-order key-state isolation `c504664`) are accepted as reported — pre-existing defects fixed on sight, outside this spec's verification scope. **Open, in order: (1) the A7 harness fix (host-selective proxy + premise assertions) and re-run — implementation; (2) maintainer ratification of the `format`/`fromDateTime`/`toDateTime` removals. D17/D18 are closed at the register on the acceptance artifacts.**

**§7 DISCHARGED, 2026-08-25.** A7 fix + re-run complete (`f5f16e2`, `e65a654`): host-selective proxy instrument with its own localhost-only unit tests (`test_acceptance_proxy_instrument.py`), premise-gated probe (premises not established → VOID, never PASS/FAIL — F23 honored in the instrument's own contract), clamp baseline reproduced on 3.10 and 3.14. **A1–A8 all pass live; every step of the work order is done and adjudicated.** The report's two "open reconciliation items" were already settled before it was written (`152ea1f`): the token `skip` is ratified in §6.3 and the F38 code split is ratified into `fulltext` §9. Remaining, outside this work order: **F39** (the committed live key — rotation with the maintainer, fixture replacement + tree-scan guard test with implementation; `fulltext/14-defect-priority.md` §18) and the maintainer's ratification of the `format`/`fromDateTime`/`toDateTime` removals (§6.1). D17/D18 are closed at the register.

**Addendum 2 — time-bounding restoration (issued 2026-08-25, post-discharge; a small follow-up, not a reopening).** Per Q10: restore `fromDateTime`/`toDateTime` per the §6.1 ruling — client-side validation (ISO date/datetime, truncate to date, `from ≤ to`), `publishdate:range` assembly with native one-sided forms, fallback `updateDate` client-side filter with the named semantic difference, description item 8. Tests: validation and truncation; assembly including both one-sided shapes; the fallback filter; disclosure strings. Acceptance: **A9** (§3) live. While in the file, the F39 follow-up can ride along if the maintainer sequences it here: fake-by-construction fixture replacement + the tree-scan guard test (`fulltext/14-defect-priority.md` F39).

**Addendum 2 + F39 ride-along DISCHARGED, 2026-08-25** (`f7f53e7`, `c3d4c97`, `0c706de`; adjudicated by this session). Time-bounding verified live — **A9 all-pass with A1–A8 re-confirmed** (`runs/govinfo-search/2026-08-25T032736Z/`, key-absence grep-verified); the fallback disclosure leg ran under the host-selective poison with attribution held, and the structured `date_bounds {from, to, applied_to}` shape is accepted as an improvement on the ruling (machine-readable where the ruling asked only for a named difference). The A9 granularity correction is recorded in §3 — the spec's error, the harness's catch. **F39 verified and CLOSED** (`fulltext/14-defect-priority.md`): this session measured the old key at **0 tracked occurrences** (`git grep`) and ran the guard test black-box (3 passed) — key-shaped-but-visibly-fake fixture preserving the secret round-trip audits, exactly-40 three-character-class scan with comment-required allowlist (empty), non-vacuity tested in both directions. The 3.10-floor catches (`fromisoformat` compact-format laxity on newer Pythons → regex shape-gate; a lazy-annotation-masked missing import) are accepted as reported — the floor discipline paying again. Clamp baseline reproduced by this session on the final tree. **Nothing remains open on this feature, in either session.**

## §8 Post-ship consumer critique — triage, 2026-08-25

A Claude Desktop session (via the maintainer) critiqued the shipped tool after a real RECA research task. Full triage in `tool-defect-register.md` ("Consumer critique triage"); the items in this tool's scope, each verified before recording (artifacts `runs/govinfo-search/*-critique/`):

- **Recall complaint: adjudicated NOT a defect — a documentation gap, now amended (§6.5 item 2).** The five-word query `Radiation Exposure Compensation Act amendments downwinders` returns `count: 1` from GovInfo *raw* — the tool faithfully reported upstream's implied-AND narrowing. *Deferred option, recorded not ruled (maintainer's call):* on a low-count result the tool could probe per-term document frequency (one cheap call per term, near-free under the Q7 quota reality) and report which term collapsed the set — the `query_diagnostics` idea at corpus level, for the non-zero-but-starved case A4's zero-diagnostics does not cover.
- **Snippets and scores: impossible as asked — measured.** The `/search` record's complete field set is `collectionCode, dateIngested, dateIssued, download, governmentAuthor, granuleId, lastModified, packageId, relatedLink, resultLink, title` — **no snippet, no highlight, no score**. The website's highlighting is not in the API. Exposing a snippet would mean fetching content per hit — a different, expensive feature; deferred, maintainer's call. The relevance cliff cannot be exposed because upstream does not emit it.
- **Follow-up round, 2026-08-25 (same consumer, maintainer-relayed) — dispositions:** the reshuffle claim was **retracted by its author** with the correct diagnosis (one observation, no baseline, asserted against an ordering it had no access to; a length-normalized scorer ranks the short dense bill high and one position read as a bug) — and its self-stated weighting rule is adopted for future reports from this consumer: **differential-backed observations (the F36 pair) get weight; single-observation mechanism claims get a probe before a register entry.** The snippet idea returned in a buildable form and is **ruled as Q11**; the verbose proposal was **partially retracted by its author and adopted narrowly by the maintainer as fulltext A7** (`timing` env-gated behind `CONGRESSMCP_VERBOSE`; the diagnostic-on-failure blocks stay always-on; fulltext §4 otherwise stands un-reopened); per-term DF refined into **Q12**. **For the reopened PR, the maintainer's scope ruling: only the §6.5 item-2 wording fix ships.** The consumer's push-timing advice (land register churn after the externally-aimed PR) matched the disposition already taken.
- **"Dedup reshuffles ranking": NOT REPRODUCED — and since retracted by its author (above).** The A5 acceptance artifact shows the tool's order matching raw upstream exactly (HR 10115 first on the 26-bill query), and the critique's own queries re-run raw rank the RECA-titled bills first (`crit_q1`: S 243, HR 10115, HR 1362, HR 4631 — dedup simulation preserves that order). §6.2's first-occurrence dedup preserves rank order by construction and by artifact. No recorded query — the critique's or ours — produces HR 1362 above both titled bills. Reopening requires a trace.
- **Out of this tool's scope, routed in the register:** the `amends` order-differential (F36 corroboration — the critique's strongest contribution), D20–D22 (`get_bill_details`), the multiplexer split, the `verbose` flag, and `bills_amending(cite)` (endorsed direction, F36-gated).

**Addendum 3 — the reopened-PR wording fix (issued 2026-08-25; maintainer scope ruling: this and nothing else rides the reopened PR).** **ADJUDICATED NOT DISCHARGED 2026-08-25 — the report and the tree diverge.** The report claims the bullet is "§6.5 item 2 verbatim"; a string-level check (the F39 grep instrument — no source read) finds **every distinctive phrase of the adopted bullet absent from the shipped tree**: `downwinders`, `strictly shrinks`, `Worked example`, `starved`, `synonym`, `intersects and discards` — all 0 in `bills_tool.py`, present only in this spec. What shipped (10-line diff, `683fe53`) carries only the *retained* clauses ("ANDed", "do not quote") — i.e., **§6.5 item 2's framing sentence was read as the content and the blockquoted normative text was missed.** The re-pinned description test is green and proves nothing: it was authored from the same misreading — a test and the change it guards sharing one mental model, the standing `fulltext/00-INDEX.md` rule, here on a *documentation* change. **Bounce, with mechanical acceptance:** the docstring bullet is the §6.5 item-2 **blockquote**, verbatim (re-wrapped only); the description test pins the worked example, the starvation clause, and the synonyms line *specifically*; acceptance = `git grep` finds `downwinders`, `intersects and discards`, and `strictly shrinks` in `bills_tool.py`. **The squash-and-reopen waits on this** — squashing now would ship a PR whose single stated purpose is absent from it. **Round 2 (`d0b5110`) ADJUDICATED DISCHARGED 2026-08-25:** the three-phrase acceptance passes on the committed tree, verified independently by this session (`downwinders` / `intersects and discards` / `strictly shrinks` — 1 hit each in `bills_tool.py`); clamp baseline reproduced. The re-authored pin test is accepted *with its reported failure credited as the evidence*: it failed once against the shipped text before going green, so it demonstrably discriminates — which round 1's never did; a pin test's first failure is its proof of life. The implementer's own read of round 1 is recorded as given: the blockquote's thesis — the abstract statement gets read and ignored, the concrete example changes behavior — applied to its implementer before any caller. **Addendum 3 complete; the reopened PR's payload is real.** Replace the `search_bills` docstring's matching-semantics bullet with the §6.5 item-2 normative text verbatim (keep the existing voice and wrap; the no-quotes warning, `title:`/`shorttitle:` forms, and operator pass-through stay). One test touch: the description-pinning test updates to the new bullet. **Deliberately out of the reopened PR:** Q11 snippets, Q12 diagnostics, A7's `CONGRESSMCP_VERBOSE` timing gate (fulltext §4) — all approved directions, next batch, sequenced by the maintainer. Rebase note: master has moved (#70 fixes the same 3.10 `test_bucket_double_conversion` bug this branch fixed in-squash — expect a duplicate-fix conflict to resolve by taking upstream's; #72 touches the shared API wrapper the govinfo client calls through — re-run the failure-flow tests after rebase).

**Post-merge check, 2026-08-26 (maintainer flagged the docstring as still misleading; measured by the spec session, string-level on `master` — the F39 grep instrument, no source read).** The §6.5 item-2 blockquote's load-bearing content **is present in the merged `congress_api/features/bills_tool.py`**: `words are ANDed`, `SAME document`, `strictly shrinks`, `can never grow`, `distinctive minimum`, `Do NOT add words describing`, `usual cause of a starved result`, `downwinders" returns 1 bill`, `returns 26`, `enacted vehicle`, `A small count means`, `rarely co-occur`, `Synonyms do NOT broaden`, `intersects and discards` — 1 hit each. Two exact strings drifted in re-wrap (`Worked example` label, `re-query narrower` — 0 hits each); the three deliberately-load-bearing properties (worked example with magnitudes, starvation clause, cross-tool boundary) all survive, so no bounce on the semantics bullet. *(Corrected 2026-08-27: both 0-hit phrases were present but split across the docstring's 80-column wrap — a single-line grep is blind to wrapped phrases, an instrument limitation to remember — and "re-query narrower" was additionally* wrong*; see the §6.5 item-2 correction note.)* **What is measurably absent is the fields documentation: only 5 of §2a's 18 BILLS fielded operators appear in the file** (`title:` `shorttitle:` `congress:` `billtype:` `docnumber:`; zero hits for `billversion:` `chamber:` `member:` `memberparty:` `memberstate:` `committee:` `actiondate:` `publishdate:` `isprivate:` `isappropriation:` `uscodecitation:` `statutecitation:` `plawcitation:`). "Operator pass-through" without the operator list is the D17 root harm again — semantics the caller was never told. Re-issued as **Addendum 4 item 0**, top priority. *(If a live session also saw the old matching-semantics text, check the installed release: the v2.2.0 bump predates #73, so a pip-installed server can lag master.)*

**Addendum 4 — the approved next batch (issued 2026-08-26; item 0 added same day): the docstring fields fix, Q11 local snippets, Q12 zero-result term diagnostics, the fulltext A7 timing gate.** Items 1–3 are what Addendum 3 deliberately held out of the reopened PR, all maintainer-approved directions; **item 0 is the Addendum 3 post-merge finding and goes first — it misleads a model right now.** The reopened PR **merged upstream as #73** (`38c2930` on master — observed from git history by the spec session), so the batch is unblocked. The items are independent; Q12's scheduling was recorded as the maintainer's call, so if the maintainer wants it held, strike item 2 — the others stand alone. Item 0 is small enough to ship on its own branch ahead of the rest; new feature branch(es) off master; unit tests per `AGENTS.md`; the e2e harness stays with the maintainer.

**Item 0 — `search_bills` docstring: enumerate the supported fields (maintainer requirement, 2026-08-26; top priority — the current docstring misleads by omission).** The Addendum 3 post-merge check above is the measurement: the docstring says operators pass through but names only 5 of the 18 §2a BILLS fields, so a model either never uses the other 13 or invents field names — and an invented field's failure shape is unknown. §6.5 gains **item 9**, mandatory like the rest:

- **The docstring enumerates every supported fielded operator with its value form** — the §2a set: `congress:` `billtype:` `docnumber:` `billversion:` `title:` `shorttitle:` `chamber:` `member:` `memberparty:` `memberstate:` `committee:` `actiondate:` `publishdate:` `isprivate:` `isappropriation:` `uscodecitation:` `statutecitation:` `plawcitation:` — plus the general query-language affordances already shipped behaviorally (quoted phrases with the title caveat, `AND`/`OR`/`NOT`, `field:range(a,b)`).
- **The standing per-docs rule binds: one confirming probe per newly documented field before the docstring names it** (§2a is vendor documentation — a claim, not a measurement; `congress:`/`billtype:`/`docnumber:`/`title:`/`shorttitle:` are already measured). Each probe is a known-positive query expected non-zero, artifacts under `runs/govinfo-search/`; a field that fails its probe is **omitted from the docstring and recorded here**, not documented on faith. The probe also settles each field's accepted value syntax (e.g., what string `uscodecitation:` actually takes) — the docstring states the measured form, not the guessed one.
- **One extra probe, preregistered: what does an unrecognized field name do upstream?** Expected: silently matches nothing (fielded terms AND like words). If confirmed, the docstring says so in one line — "unlisted fields are not a search error; they match nothing" — because that failure is a silent zero, exactly the class this tool's three-zeros rule exists to kill. If it errors instead, record the shape and skip the line. **Preregistration FALSIFIED 2026-08-27:** an unrecognized field is an upstream **HTTP 500** with a generic body (`unrecognized_field_prereg.json` in the probe run — the §2b malformed-request family), not silent-empty. The implementation skipped the "matches nothing" line per the contract and shipped a one-sentence warning not to invent field names; **ratified, with the normative wording fixed here:** the docstring states that field names outside the documented list are an upstream error (surfaced per §6.4, which for a 5xx means the labeled fallback path) — so an invented field does not search the corpus at all; do not invent field names. Acceptance grep: `invent` present in `bills_tool.py`.
- **Note for the register:** `uscodecitation:`/`plawcitation:`/`statutecitation:` are the F36-adjacent affordance §2a flagged — documenting them is the first concrete step toward the endorsed `bills_amending(cite)` direction (F36-gated, at the register).
- **Tests:** the description-pin test extends to the field list (each field name pinned); acceptance = `git grep` finds every documented field name in `bills_tool.py`, probe artifacts exist per field, and the unrecognized-field line matches its probe.

**Item 1 — Q11 local snippets.** The §8 Q11 ruling is the contract; this operationalizes it.

- **Two composable modes, both as ruled:** (a) opportunistic, always on, cache-only — a hit whose package is already in the persistent cache gets a snippet for free, zero network; (b) opt-in bounded fetch — request parameter `snippet_fetch: N`, default 0, hard cap 5, which downloads-and-parses the top-N uncached hits in rank order and leaves them enrolled for the follow-up reads a research session makes anyway (requested warming per Q8, not speculation).
- **Localization is per-term, not phrase** — the ruled semantic gap (GovInfo ANDs raw words; FTS5 matches stemmed phrases) is why a genuine upstream hit can fail local localization, and per-term narrows it.
- **The tri-state is structural, and it is the make-or-break.** Per hit: localized → `snippet_status: "localized"` with a `snippet` object; attempted and not localized → `snippet_status: "not_localized"` with `snippet: null`; not attempted (uncached, no opt-in) → both fields absent. An empty-string snippet must be **unrepresentable**, enforced at the serialization layer, not by convention. Per the ruling verbatim: if the tri-state cannot be kept clean, the feature does not ship.
- **New ruling — the amendatory-trap disclosure travels with the snippet.** The snippet is built from the same unit/segment machinery `search_bill_text` reads, and it carries the matched unit's `section_id` and `match_contexts`. A snippet whose matched segment is quoted must say so, and the tool description states that `"quoted"` governs — otherwise Q11 reintroduces the F4/A1 failure shape (text the bill is removing presented as what the bill says) through a side door, on the tool consumers reach *first*. This is not optional trim; a snippet without contexts does not ship.
- **Tests:** structural distinctness of all three states on one response; cache-only mode makes zero network calls (call-counter asserted, the F21 instrument pattern); `snippet_fetch` caps at 5, fetches exactly `min(N, uncached hits)` in rank order, and the fetched packages land enrolled; per-term localization matching a hit that phrase-matching would miss; empty string unrepresentable; `match_contexts` present on every emitted snippet with the quoted-governs sentence pinned in the description test.
- **Acceptance A10 (live, maintainer-run, recorded here):** one query whose response exhibits all three states — a cached hit with a snippet, an uncached hit with both fields absent, and (via `snippet_fetch` or a genuine semantics miss) a `not_localized` — artifacts under `runs/govinfo-search/`, key-absence grep as always.

**Item 2 — Q12 zero-result term diagnostics.** Refined Q12, narrowed deliberately. **SUPERSEDED 2026-08-27 by the maintainer's ladder design — see "Q12 GO" below; the error-handling and absent-unless-fired disciplines in this item carry over unchanged.**

- **Firing condition, ruled at minimal width:** the query carries **≥ 2 text terms** and the upstream total is **0**. Zero, not "low-count": at zero the caller is blind and the starvation diagnosis is unambiguous; extending to low-but-nonzero waits on a measured consumer case (the F3/F8 minimal-width precedent). A one-term zero needs no explanation and gets no probes, as ruled.
- **Shape:** a `diagnostics` object present **only when fired** — absence means not fired, never "ran and found nothing" (the three-zeros discipline). Inside: `term_counts: [{term, count}]`, one probe call per term, each probe carrying the same fielded constraints (`congress:`, date bounds, …) as the starved query so counts are comparable; a quoted phrase probes as one term. **A probe that errors must not look like a zero:** that entry ships `count: null` with `status: "probe_failed"` — the scan-that-errors convention, on the wire.
- **Tests:** fires only on multi-term zero; one-term zero and any non-zero total → no field; probe assembly preserves the fielded constraints; probe failure → `null`/`probe_failed`, never `0`; the description states the counts are per-term corpus document frequencies, not result counts.
- **Acceptance A11 (live, maintainer-run):** a genuinely starved multi-term query (the §6.5 worked-example family — a good term ANDed with a starving synonym) whose diagnostics identify the starving term.

**Item 3 — the A7 timing gate.** The contract is fulltext §4 **Amendment A7**, already ruled: `timing` is emitted only when `CONGRESSMCP_VERBOSE` is set (any non-empty value) in the server environment, on every tool that emits a timing block; `cache`, `version_resolution*`, and the diagnostic-on-failure blocks stay always-on, and §4's always-on telemetry decision is otherwise un-reopened. Whether the gate is read at startup or per call is the implementer's; what binds is the env-var contract and a README line documenting it.

- **Tests:** env unset → no `timing` key on any tool response that previously carried one; env set → `timing` present with the A6 cold/warm split intact.
- **Acceptance:** unit-level suffices — the change is subtractive; one paired live call (set/unset) rides the next harness run rather than commissioning one.

**Addendum 4 adjudication, 2026-08-27 (implementation report received; verified by this session at string/artifact level — no source read).**

- **Item 0 DISCHARGED pending merge** (branch `fix/search-bills-docstring-fields`, `aedb88b` + `3e92bf2`). Verified by this session: all 18 §2a field names present in `bills_tool.py` (`git grep`, ≥1 hit each); probe artifacts complete at `runs/govinfo-search/2026-08-27T021336Z/` (the report's `021142Z` is a citation typo) — every field non-zero on a known-positive, and **differential negatives prove `chamber:`/`billversion:`/`isprivate:`/`isappropriation:` bind rather than parse** (pos/neg pairs, 1/0), which is stronger than the work order asked. Finding 1 accepted: the first `statutecitation:` probe's 0 was a bad value choice (`104 Stat. 920` occurs in no bill's citation metadata), not a dead field — re-probed `133 Stat. 1198` → 3 hits, both artifacts archived, the docstring states the confirmed form; the ok:false first probe is archived beside the ok:true second, which is the discipline holding (a probe that fails must not vanish). Finding 2 (falsified preregistration) recorded and its replacement line ratified at item 0 above. The `re-query broader` correction (`3e92bf2`) is adjudicated at the §6.5 correction note.
- **Items 1 and 3 REPORTED IMPLEMENTED** (branch `feature/addendum4-items`: `4d27287` Q11, `8fb1c0d` A7). String-level checks by this session: the quoted-context wording is present in `bills_tool.py` and `CONGRESSMCP_VERBOSE` is in the README. The substantive claims — tri-state structural distinctness, empty-string unrepresentable at the model layer, per-term localization pinned by a test the phrase form would fail, `snippet_fetch` rank-order enrollment via the real build path, timing dropped-not-nulled per call — are consistent with the report and its test counts but are **adjudicated by A10/A11, which are maintainer-run and still owed.** Items 1/3 close on those runs.
- **Item 2 (Q12) not started — awaiting the maintainer's explicit go-ahead, as instructed.**
- **Merge sequencing note (implementer-flagged, recorded):** `fix/search-bills-docstring-fields` and `feature/addendum4-items` both edit the `SEARCH_BILLS` docstring block; whichever merges second hits a small mechanical conflict in `bills_tool.py` — both edits survive, nothing overlaps semantically.
- **Test-suite claim accepted as reported:** three branches green on the maintainer's venv and fresh 3.10/3.12 venvs, known-failure set unchanged; the worktree-only `cache_cli` console failure diagnosed as cross-checkout contamination via the editable install (an environment-dependent instrument failure, the F37 family — worth remembering when a failure appears only in a worktree).

**Addendum 4, continued — 2026-08-27: A10 adjudicated (items 1 and 3 close); Q12 GO with the ladder design.**

**A10 PASS, adjudicated by this session** (`runs/govinfo-search/2026-08-27T034403Z/`, read directly): one response exhibits all three snippet states with zero structural violations — `localized` beside a genuine `not_localized` (the semantics-gap state, produced live) beside `absent`; the empty-cache run shows absent-everywhere; `snippet_fetch: 2` localizes and enrolls the top 2, and the `snippet_fetch: 0` re-run still carries their snippets, proving the cache is the only possible source. A1–A9 re-confirmed in the same run. **Item 1 (Q11) CLOSED.** **Item 3 (A7 gate) CLOSED at its ruled unit-level acceptance;** the optional paired live call still rides a future harness run, non-blocking.

**Q12 GO — the maintainer's design, ruled 2026-08-27, superseding item 2's per-term document-frequency shape.** The maintainer supplied the product call the zero-only threshold was explicitly waiting on, and a better instrument with it: **fire when the query has ≥ 2 text terms and the upstream total is < 10 (0 included); diagnose by chopping text terms from the right, one per rung, re-querying until one term remains, and reporting the count at every rung.**

Why the ladder supersedes per-term DF — recorded so the reversal is legible: **per-term counts do not compose under AND.** Every term can be individually common while the intersection is empty (the synonym case §6.5 warns about is exactly this), so per-term DF can report "nothing starved" on a starved query. The ladder measures the *actual intersections*, and it operationalizes the docstring's own worked example (chop from the right: 1 → 26) and its corrected remedy ("re-query broader (drop terms)") — the diagnostic and the description now teach the same move, and each rung is a remediation the caller can take verbatim.

Contract:

- **Chop units are text terms only.** A quoted phrase is one unit. Fielded operators (`congress:` …) and date bounds are constraints, not text terms — they are held fixed on every rung and never chopped.
- **Rung 0 is the full query with the count already in hand** — no extra call; the ladder is self-contained reading top to bottom. Then one rung per chop down to a single term: cost n−1 calls, same order as the superseded design, near-free per the Q7 deployment reality. Every rung sends the same sorts and constraints as the main query.
- **Field shape:** `diagnostics.term_ladder: [{terms, count}]` in chop order, present **only when fired** — absence still means not fired, never "ran and found nothing." A rung whose probe errors ships `count: null` with `status: "probe_failed"`, never `0`, and the ladder continues past it. A ladder failure must not alter the main response beyond the diagnostics field — the primary result is already in hand.
- **Order caveat, stated in the description:** right-chopping isolates trailing added words — the measured usual failure. When the rare term sits first, every rung stays small down to the single-term rung, which is itself the correct reading: the core term is rare. The description also states that a small count can be a *correct* answer — the ladder is evidence for judging a result, not a verdict against it.
- **Tests:** fires only on multi-term + total < 10; one-term and total ≥ 10 → no field; constraints and sorts preserved on every rung; quoted phrase chops as one unit; probe failure → `null`/`probe_failed`, ladder continues; rung 0 consumes no extra call; description pins the order caveat and the small-can-be-correct line.
- **A11, redefined against the ladder (maintainer-run once Q12 ships — it could not exist before it):** a starved multi-term query of the worked-example family whose ladder shows the collapse rung, plus a one-term small-count query showing no field, plus one constraint-starved query (below) whose leave-one-out names the dead constraint.

**Constraints leg — ruled 2026-08-27, answering the maintainer's operator question.** Operators are **not** chopped inside the ladder: they have no natural chop order, and mixing them into the text rungs would conflate two different diagnoses. But the fixed-constraints ladder has a measurable failure mode the maintainer's version example exposes — a constraint that matches nothing (e.g. `billversion:enr` on a bill with no enrolled version) makes *every* rung small, and the ladder then misreads as "your terms are rare." The constraint case is diagnosed separately:

- **Terminal rung, whenever the ladder fires:** after the single-term rung, one **constraints-only** probe — the caller's constraints with no text terms at all. This is the already-measured version-discovery query shape (§6.5 item 3) generalized: its count is the size of the constrained universe, the ladder's true denominator ("2,000 bills match your constraints; your terms cut it to 3"). One call.
- **Leave-one-out, fired only at total == 0:** one probe per droppable constraint, each omitting exactly that constraint, reported as `{omitted, count}` — the omission that restores hits names the culprit. Fires for *any* zero-total query carrying ≥ 1 droppable constraint, **including pure fielded queries with 0–1 text terms that the text ladder never fires on** — which is precisely the nonexistent-version shape. Threshold deliberately 0, not < 10: constraint starvation is binary in practice (a constraint scopes to the thing or it doesn't), and healthy fielded queries legitimately return small counts — version sets measured max 5 — so a < 10 trigger would decorate nearly every version-discovery call with probe noise.
- **Droppable:** caller-typed fielded operators, the structured params (`congress`, `bill_type`, …), and the date bounds. **Never droppable:** the `collection:bills` corpus scope and the assembled sorts. A quoted phrase or parenthesized boolean group is one unit in both legs; nothing is ever split inside one — the same tokenizer that identifies text-term chop units identifies these.
- **The semantics worry is answered by labeling, and only by labeling:** every probe reports exactly what was omitted, in the diagnostics field only; the main results never re-scope. A chopped-constraint count answers a diagnostic question ("what would match without this constraint"), and the field says which question each number answers — the silent-re-scope failure cannot occur because nothing unlabeled exists.
- **Tests added:** constraints-only terminal rung present whenever the ladder fires; leave-one-out only at zero and never dropping the corpus scope; a pure fielded zero (nonexistent version) fires leave-one-out without a text ladder; group/phrase units never split; each probe labeled with its omission.

**A11 adjudicated PASS — Q12 CLOSED; Addendum 4 COMPLETE (2026-08-27), pending one merge.** Adjudicated by this session on the full A1–A11 run `runs/govinfo-search/2026-08-27T041618Z/` (ALL PASS, A1–A10 re-confirmed beside it; `041207Z` is the implementer's A11-only shakedown with identical A11 results; the maintainer's citation of `034403Z` for A11 was a path slip — that directory is the pre-Q12 A10 run and contains no A11 artifacts, verified). Read from the artifacts directly:

- **Ladder cell:** the contract exactly — labeled `terms` per rung, rung 0 the main count with no extra call, right-chop to one term, terminal `terms: []` denominator. Worked-example query: **1 → 24 → 32 → 32 → 50 → 104 → 21,337**, collapse visible on the first chop — the docstring's own example, now measured by the tool itself.
- **One-term cell:** total 1, `has_diagnostics: False` — the fire condition holding on the other side.
- **Dead-constraint cell:** `billversion:enr` on unenrolled 119 hr4631 (total 0) → labeled leave-one-out: omit `billversion:enr` → 1, omit `congress:119` → 1 (an earlier congress's enrolled HR 4631 — the labeled-question property demonstrated live), omit `billtype:hr` → 0, omit `docnumber:4631` → 51.
- **Key hygiene, checked by this session:** the only 40-char strings in either run dir are base64 page tokens (decode to `{"offsetMark"…`), not key-shaped secrets.
- **Pins verified string-level on the integrate branch:** both mandated description sentences pinned; `term_ladder` throughout the diagnostics tests.

**One implementation choice ratified with a recorded gap:** `NOT x`/`-x` exclusions are held fixed in the ladder and excluded from the terminal rung — right on both counts (they are neither text terms nor constraints). The gap: an exclusion that itself starves the set is diagnosed by **neither** leg. Accepted at minimal width — caller-typed exclusions are rare on this surface — extension gated on an observed consumer case, the standing pattern.

**Placement ratified; the merge-sequencing concern is superseded.** Built on `feature/addendum4-items`, cherry-picked to `integrate/f36-docstring-addendum4`, which this session verified contains all three packages **and** the spec commits through `d1c6254`. What remains: **merge the integrate branch**, plus the spec branch tip (this adjudication rides `fix/f36-amends-extraction` and post-dates the integrate cherry-picks — refresh or merge both). Test-count claim accepted as reported (989 on 3.14 / 987 on 3.10, pre-existing baseline unchanged).

**Addendum 4 is COMPLETE: items 0–3 delivered, adjudicated, accepted.**

